[build]
    functions = "functions"
[[redirects]]
    from = "api/*"
    to= ".netlify/functions/:splat"
    status = 200

[functions."schedule"]
schedule = "@hourly"

[functions]
  node_bundler = "esbuild"